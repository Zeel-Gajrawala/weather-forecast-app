import { CustomizeDatePipe } from './customize-date.pipe';

describe('CustomizeDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomizeDatePipe();
    expect(pipe).toBeTruthy();
  });
});
